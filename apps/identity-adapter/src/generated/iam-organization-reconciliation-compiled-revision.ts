/*
 * Generated only by scripts/generate-iam-organization-reconciliation-compiled-revision.mjs
 * during an immutable container build. The committed source is deliberately
 * fail-closed so an ordinary local TypeScript build cannot attest a revision.
 */
export const ORGANIZATION_RECONCILIATION_COMPILED_BUILD_REVISION: string | null = null;
